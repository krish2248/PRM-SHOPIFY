{%- comment -%}
  About Us Page Template
  Showcases company history, values, and team
{%- endcomment -%}

<div class="about-page">
  {%- comment -%} Hero Section {%- endcomment -%}
  <section class="about-hero">
    <div class="container">
      <div class="about-hero__content">
        <span class="about-hero__label">{{ section.settings.hero_label | default: 'Our Story' }}</span>
        <h1 class="about-hero__title">{{ section.settings.hero_title | default: 'Rooted in Tradition, Driven by Wellness' }}</h1>
        <p class="about-hero__text">{{ section.settings.hero_text | default: 'Since our founding, PRM HerboVilla has been committed to bringing authentic Ayurvedic wellness solutions to modern families.' }}</p>
      </div>
    </div>
  </section>

  {%- comment -%} Mission Section {%- endcomment -%}
  <section class="about-mission section">
    <div class="container">
      <div class="about-mission__grid">
        <div class="about-mission__image">
          {%- if section.settings.mission_image != blank -%}
            <img src="{{ section.settings.mission_image | image_url: width: 600 }}" 
                 alt="{{ section.settings.mission_image.alt | default: 'Our Mission' }}"
                 loading="lazy">
          {%- else -%}
            <div class="about-mission__placeholder">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
              </svg>
            </div>
          {%- endif -%}
        </div>
        <div class="about-mission__content">
          <span class="about-mission__label">{{ section.settings.mission_label | default: 'Our Mission' }}</span>
          <h2 class="about-mission__title">{{ section.settings.mission_title | default: 'Bringing Ayurveda to Every Home' }}</h2>
          <div class="about-mission__text">
            {{ section.settings.mission_text | default: '<p>At PRM HerboVilla, we believe that nature holds the key to optimal health. Our mission is to make authentic Ayurvedic remedies accessible to everyone, without compromising on quality or purity.</p><p>Every product we create is a result of extensive research, traditional knowledge, and modern manufacturing practices that ensure safety and efficacy.</p>' }}
          </div>
        </div>
      </div>
    </div>
  </section>

  {%- comment -%} Values Section {%- endcomment -%}
  <section class="about-values section section--alt">
    <div class="container">
      <div class="section-header section-header--center">
        <span class="section-header__label">{{ section.settings.values_label | default: 'What We Stand For' }}</span>
        <h2 class="section-header__title">{{ section.settings.values_title | default: 'Our Core Values' }}</h2>
      </div>
      
      <div class="about-values__grid">
        {%- for block in section.blocks -%}
          {%- if block.type == 'value' -%}
            <div class="value-card" {{ block.shopify_attributes }}>
              <div class="value-card__icon">
                {%- case block.settings.icon -%}
                  {%- when 'leaf' -%}
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                      <path d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.48 19 2c1 2 2 4.18 2 8 0 5.5-4.78 10-10 10Z"/>
                      <path d="M2 21c0-3 1.85-5.36 5.08-6C9.5 14.52 12 13 13 12"/>
                    </svg>
                  {%- when 'shield' -%}
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                      <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
                      <path d="m9 12 2 2 4-4"/>
                    </svg>
                  {%- when 'heart' -%}
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                      <path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/>
                    </svg>
                  {%- when 'users' -%}
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                      <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
                      <circle cx="9" cy="7" r="4"/>
                      <path d="M22 21v-2a4 4 0 0 0-3-3.87"/>
                      <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                    </svg>
                  {%- when 'award' -%}
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                      <circle cx="12" cy="8" r="6"/>
                      <path d="M15.477 12.89 17 22l-5-3-5 3 1.523-9.11"/>
                    </svg>
                  {%- else -%}
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                      <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
                    </svg>
                {%- endcase -%}
              </div>
              <h3 class="value-card__title">{{ block.settings.title }}</h3>
              <p class="value-card__text">{{ block.settings.text }}</p>
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>
  </section>

  {%- comment -%} Story Timeline {%- endcomment -%}
  <section class="about-story section">
    <div class="container">
      <div class="section-header section-header--center">
        <span class="section-header__label">{{ section.settings.story_label | default: 'Our Journey' }}</span>
        <h2 class="section-header__title">{{ section.settings.story_title | default: 'The PRM HerboVilla Story' }}</h2>
      </div>
      
      <div class="about-story__content">
        <div class="about-story__text rte">
          {{ section.settings.story_content | default: '<p>Founded in the heart of Gujarat, PRM HerboVilla began with a simple vision: to bring the healing power of Ayurveda to every household. What started as a small manufacturing unit has grown into a trusted name in herbal wellness.</p><p>Our journey has been guided by the principles of Ayurveda — balance, purity, and harmony with nature. Every formulation we create honors centuries of traditional knowledge while meeting modern standards of quality and safety.</p><p>Today, we are proud to serve thousands of customers across India, helping them achieve their health goals naturally. Our state-of-the-art facility in Bhavnagar combines traditional Ayurvedic practices with modern manufacturing excellence.</p>' }}
        </div>
      </div>
    </div>
  </section>

  {%- comment -%} Manufacturing Section {%- endcomment -%}
  <section class="about-manufacturing section section--alt">
    <div class="container">
      <div class="about-manufacturing__grid">
        <div class="about-manufacturing__content">
          <span class="about-manufacturing__label">{{ section.settings.mfg_label | default: 'Quality Assurance' }}</span>
          <h2 class="about-manufacturing__title">{{ section.settings.mfg_title | default: 'Our Manufacturing Excellence' }}</h2>
          <div class="about-manufacturing__text rte">
            {{ section.settings.mfg_text | default: '<p>Our manufacturing facility is located in the Gujarat Industrial Development Corporation area in Bhavnagar. We follow strict GMP (Good Manufacturing Practices) guidelines to ensure every product meets the highest quality standards.</p>' }}
          </div>
          
          <ul class="about-manufacturing__features">
            {%- for block in section.blocks -%}
              {%- if block.type == 'feature' -%}
                <li class="about-manufacturing__feature" {{ block.shopify_attributes }}>
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M20 6 9 17l-5-5"/>
                  </svg>
                  <span>{{ block.settings.text }}</span>
                </li>
              {%- endif -%}
            {%- endfor -%}
          </ul>
        </div>
        <div class="about-manufacturing__image">
          {%- if section.settings.mfg_image != blank -%}
            <img src="{{ section.settings.mfg_image | image_url: width: 600 }}" 
                 alt="{{ section.settings.mfg_image.alt | default: 'Our Manufacturing Facility' }}"
                 loading="lazy">
          {%- else -%}
            <div class="about-manufacturing__placeholder">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M2 20a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8l-7 5V8l-7 5V4a2 2 0 0 0-2-2H4a2 2 0 0 0-2 2Z"/>
                <path d="M17 18h1M12 18h1M7 18h1"/>
              </svg>
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>
  </section>

  {%- comment -%} Contact CTA {%- endcomment -%}
  <section class="about-cta section">
    <div class="container">
      <div class="about-cta__content">
        <h2 class="about-cta__title">{{ section.settings.cta_title | default: 'Have Questions? We\'re Here to Help' }}</h2>
        <p class="about-cta__text">{{ section.settings.cta_text | default: 'Our team is ready to assist you on your wellness journey. Reach out to us for product recommendations or any queries.' }}</p>
        <div class="about-cta__buttons">
          <a href="{{ section.settings.cta_button_url | default: '/pages/contact' }}" class="btn btn--primary">
            {{ section.settings.cta_button_text | default: 'Contact Us' }}
          </a>
          <a href="{{ section.settings.cta_button_2_url | default: '/collections/all' }}" class="btn btn--outline">
            {{ section.settings.cta_button_2_text | default: 'Shop Products' }}
          </a>
        </div>
      </div>
    </div>
  </section>
</div>

<style>
  .about-page {
    --about-spacing: 5rem;
  }

  /* Hero */
  .about-hero {
    background: linear-gradient(135deg, var(--color-primary) 0%, #1e4d3a 100%);
    color: var(--color-white);
    padding: 5rem 0;
    text-align: center;
  }

  .about-hero__label {
    display: inline-block;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    opacity: 0.9;
    margin-bottom: 1rem;
  }

  .about-hero__title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 600;
    margin-bottom: 1.5rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .about-hero__text {
    font-size: 1.125rem;
    opacity: 0.9;
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* Mission Section */
  .about-mission__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .about-mission__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .about-mission__image img,
  .about-mission__placeholder {
    width: 100%;
    border-radius: var(--radius-lg);
    aspect-ratio: 4/3;
    object-fit: cover;
  }

  .about-mission__placeholder {
    background: var(--color-background-alt);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .about-mission__placeholder svg {
    width: 64px;
    height: 64px;
    color: var(--color-primary);
    opacity: 0.5;
  }

  .about-mission__label {
    display: inline-block;
    font-size: 0.875rem;
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
  }

  .about-mission__title {
    font-family: var(--font-heading);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    color: var(--color-text);
    margin-bottom: 1.5rem;
  }

  .about-mission__text {
    color: var(--color-text-light);
    line-height: 1.8;
  }

  .about-mission__text p + p {
    margin-top: 1rem;
  }

  /* Values Section */
  .about-values__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .value-card {
    background: var(--color-white);
    padding: 2rem;
    border-radius: var(--radius-lg);
    text-align: center;
    box-shadow: 0 2px 8px rgba(0,0,0,0.04);
  }

  .value-card__icon {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, var(--color-primary) 0%, #1e4d3a 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
  }

  .value-card__icon svg {
    width: 28px;
    height: 28px;
    color: var(--color-white);
  }

  .value-card__title {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    color: var(--color-text);
    margin-bottom: 0.75rem;
  }

  .value-card__text {
    color: var(--color-text-light);
    font-size: 0.9375rem;
    line-height: 1.7;
  }

  /* Story Section */
  .about-story__content {
    max-width: 800px;
    margin: 3rem auto 0;
  }

  .about-story__text {
    color: var(--color-text-light);
    font-size: 1.0625rem;
    line-height: 1.9;
  }

  .about-story__text p + p {
    margin-top: 1.5rem;
  }

  /* Manufacturing Section */
  .about-manufacturing__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .about-manufacturing__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .about-manufacturing__label {
    display: inline-block;
    font-size: 0.875rem;
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
  }

  .about-manufacturing__title {
    font-family: var(--font-heading);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    color: var(--color-text);
    margin-bottom: 1.5rem;
  }

  .about-manufacturing__text {
    color: var(--color-text-light);
    line-height: 1.8;
    margin-bottom: 2rem;
  }

  .about-manufacturing__features {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .about-manufacturing__feature {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 0;
    color: var(--color-text);
  }

  .about-manufacturing__feature svg {
    width: 20px;
    height: 20px;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .about-manufacturing__image img,
  .about-manufacturing__placeholder {
    width: 100%;
    border-radius: var(--radius-lg);
    aspect-ratio: 4/3;
    object-fit: cover;
  }

  .about-manufacturing__placeholder {
    background: var(--color-white);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .about-manufacturing__placeholder svg {
    width: 64px;
    height: 64px;
    color: var(--color-primary);
    opacity: 0.5;
  }

  /* CTA Section */
  .about-cta {
    background: linear-gradient(135deg, var(--color-primary) 0%, #1e4d3a 100%);
    color: var(--color-white);
  }

  .about-cta__content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .about-cta__title {
    font-family: var(--font-heading);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    margin-bottom: 1rem;
  }

  .about-cta__text {
    font-size: 1.0625rem;
    opacity: 0.9;
    margin-bottom: 2rem;
    line-height: 1.7;
  }

  .about-cta__buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }

  .about-cta .btn--primary {
    background: var(--color-white);
    color: var(--color-primary);
  }

  .about-cta .btn--primary:hover {
    background: var(--color-background);
  }

  .about-cta .btn--outline {
    border-color: var(--color-white);
    color: var(--color-white);
  }

  .about-cta .btn--outline:hover {
    background: var(--color-white);
    color: var(--color-primary);
  }
</style>

{% schema %}
{
  "name": "About Page",
  "tag": "section",
  "class": "section-about",
  "settings": [
    {
      "type": "header",
      "content": "Hero Section"
    },
    {
      "type": "text",
      "id": "hero_label",
      "label": "Hero Label",
      "default": "Our Story"
    },
    {
      "type": "text",
      "id": "hero_title",
      "label": "Hero Title",
      "default": "Rooted in Tradition, Driven by Wellness"
    },
    {
      "type": "textarea",
      "id": "hero_text",
      "label": "Hero Text",
      "default": "Since our founding, PRM HerboVilla has been committed to bringing authentic Ayurvedic wellness solutions to modern families."
    },
    {
      "type": "header",
      "content": "Mission Section"
    },
    {
      "type": "image_picker",
      "id": "mission_image",
      "label": "Mission Image"
    },
    {
      "type": "text",
      "id": "mission_label",
      "label": "Mission Label",
      "default": "Our Mission"
    },
    {
      "type": "text",
      "id": "mission_title",
      "label": "Mission Title",
      "default": "Bringing Ayurveda to Every Home"
    },
    {
      "type": "richtext",
      "id": "mission_text",
      "label": "Mission Text",
      "default": "<p>At PRM HerboVilla, we believe that nature holds the key to optimal health. Our mission is to make authentic Ayurvedic remedies accessible to everyone, without compromising on quality or purity.</p><p>Every product we create is a result of extensive research, traditional knowledge, and modern manufacturing practices that ensure safety and efficacy.</p>"
    },
    {
      "type": "header",
      "content": "Values Section"
    },
    {
      "type": "text",
      "id": "values_label",
      "label": "Values Label",
      "default": "What We Stand For"
    },
    {
      "type": "text",
      "id": "values_title",
      "label": "Values Title",
      "default": "Our Core Values"
    },
    {
      "type": "header",
      "content": "Story Section"
    },
    {
      "type": "text",
      "id": "story_label",
      "label": "Story Label",
      "default": "Our Journey"
    },
    {
      "type": "text",
      "id": "story_title",
      "label": "Story Title",
      "default": "The PRM HerboVilla Story"
    },
    {
      "type": "richtext",
      "id": "story_content",
      "label": "Story Content",
      "default": "<p>Founded in the heart of Gujarat, PRM HerboVilla began with a simple vision: to bring the healing power of Ayurveda to every household. What started as a small manufacturing unit has grown into a trusted name in herbal wellness.</p><p>Our journey has been guided by the principles of Ayurveda — balance, purity, and harmony with nature. Every formulation we create honors centuries of traditional knowledge while meeting modern standards of quality and safety.</p><p>Today, we are proud to serve thousands of customers across India, helping them achieve their health goals naturally. Our state-of-the-art facility in Bhavnagar combines traditional Ayurvedic practices with modern manufacturing excellence.</p>"
    },
    {
      "type": "header",
      "content": "Manufacturing Section"
    },
    {
      "type": "image_picker",
      "id": "mfg_image",
      "label": "Manufacturing Image"
    },
    {
      "type": "text",
      "id": "mfg_label",
      "label": "Manufacturing Label",
      "default": "Quality Assurance"
    },
    {
      "type": "text",
      "id": "mfg_title",
      "label": "Manufacturing Title",
      "default": "Our Manufacturing Excellence"
    },
    {
      "type": "richtext",
      "id": "mfg_text",
      "label": "Manufacturing Text",
      "default": "<p>Our manufacturing facility is located in the Gujarat Industrial Development Corporation area in Bhavnagar. We follow strict GMP (Good Manufacturing Practices) guidelines to ensure every product meets the highest quality standards.</p>"
    },
    {
      "type": "header",
      "content": "CTA Section"
    },
    {
      "type": "text",
      "id": "cta_title",
      "label": "CTA Title",
      "default": "Have Questions? We're Here to Help"
    },
    {
      "type": "textarea",
      "id": "cta_text",
      "label": "CTA Text",
      "default": "Our team is ready to assist you on your wellness journey. Reach out to us for product recommendations or any queries."
    },
    {
      "type": "text",
      "id": "cta_button_text",
      "label": "CTA Button Text",
      "default": "Contact Us"
    },
    {
      "type": "url",
      "id": "cta_button_url",
      "label": "CTA Button URL"
    },
    {
      "type": "text",
      "id": "cta_button_2_text",
      "label": "CTA Button 2 Text",
      "default": "Shop Products"
    },
    {
      "type": "url",
      "id": "cta_button_2_url",
      "label": "CTA Button 2 URL"
    }
  ],
  "blocks": [
    {
      "type": "value",
      "name": "Core Value",
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "leaf", "label": "Leaf" },
            { "value": "shield", "label": "Shield" },
            { "value": "heart", "label": "Heart" },
            { "value": "users", "label": "Users" },
            { "value": "award", "label": "Award" }
          ],
          "default": "leaf"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Natural Ingredients"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "We source only the finest natural herbs and ingredients for our formulations."
        }
      ]
    },
    {
      "type": "feature",
      "name": "Manufacturing Feature",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Feature Text",
          "default": "GMP Certified Facility"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "About Page",
      "blocks": [
        {
          "type": "value",
          "settings": {
            "icon": "leaf",
            "title": "100% Natural",
            "text": "Every ingredient is sourced from nature, ensuring purity and authenticity in every product."
          }
        },
        {
          "type": "value",
          "settings": {
            "icon": "shield",
            "title": "Quality Assured",
            "text": "Rigorous testing and GMP compliance guarantee safety and efficacy in all our products."
          }
        },
        {
          "type": "value",
          "settings": {
            "icon": "heart",
            "title": "Customer First",
            "text": "Your wellness is our priority. We're committed to your satisfaction and health journey."
          }
        },
        {
          "type": "value",
          "settings": {
            "icon": "award",
            "title": "Traditional Wisdom",
            "text": "Formulations based on ancient Ayurvedic texts, validated by modern science."
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "GMP Certified Manufacturing Facility"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "ISO Quality Management Standards"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "AYUSH Ministry Licensed"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "100% Quality Tested Raw Materials"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "State-of-the-Art Laboratory Testing"
          }
        }
      ]
    }
  ]
}
{% endschema %}
